# Installation methods

This package is not on the general repository, but it can be installed from its
source on GitHub by typing  the following in a Julia console.

```julia
using Pkg ; Pkg.add(url="https://github.com/dankelley/GliderAnalysis.jl")
```
